from setuptools import find_packages, setup

setup(
    name='netbox-user',
    version='0.2',
    description='Plugin Netbox User',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
